<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.marketmanaging">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Marketmanaging">
        <activity android:name=".CourierOrder"></activity>
        <activity android:name=".CourierRemove" />
        <activity android:name=".CourierAdd" />
        <activity android:name=".ProductRemove" />
        <activity android:name=".ProductAdd" />
        <activity android:name=".CategoryAdd" />
        <activity android:name=".CategoryRemove" />
        <activity android:name=".Password_Reset_Page" />
        <activity android:name=".MainActivity" />
        <activity android:name=".HomepageCourier" />
        <activity android:name=".HomepageManager" />
        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>